//import {expect} from './header';
//import {wrapIterable} from '../utils';

describe('utils', () => {
    describe('wrapIterable', () => {
        describe('for compatible types', () => {
            it('must wrap arrays', () => {

            });
            it('must wrap strings', () => {

            });
        });
        describe('for incompatible types', () => {
            it('must return original input', () => {
                // const input = wrapIterable();
            });
        });
    });
});
