import async from './async';
import dynamic from './dynamic';
import sync from './sync';

describe('pipe', () => {
    describe('sync', sync);
    describe('async', async);
    describe('dynamic', dynamic);
});
